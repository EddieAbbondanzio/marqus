import { createConfig } from "../../__factories__/config";
import { createIpcMainTS } from "../../__factories__/ipc";
import { useNoteIpcs } from "../../../src/main/ipcs/notes";

test("notes.delete", async () => {
  // const ipc = createIpcMainTS();
  // const config = createConfig();
  //
  // await ipc.invoke("notes.delete", "1");
  // Ensure it deletes note
  // Ensure it deletes children.
});

test("notes.moveToTrash", async () => {
  // Ensure it trashes note
  // Ensure it trashes children.
});
