import { last } from "lodash";
import { AppState } from "../../../shared/ui/app";
import { AppStateInitialDefinition } from "./1_initialDefinition";

export const APP_STATE_MIRGATIONS = [new AppStateInitialDefinition()];
